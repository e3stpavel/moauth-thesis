{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "./node_modules/.bin/astro dev",
      "name": "[identity]: debug",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}/apps/identity"
    }
  ]
}
